# Day 28 — Simple Unit Test (unittest)

## Goal
Create a minimal unit test to prove that a small function works correctly, even in a tiny project.

## What’s inside
- `add(a, b)`: a simple function that returns the sum of two numbers
- Unit tests using Python built-in `unittest`

## File
- `unittest_demo.py`

## How to Run
Option 1:
```bash
python -m unittest unittest_demo.py
